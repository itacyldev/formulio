{
  "description": "job example",
  "requiredContexts": [
    "user",
    "job"
  ],
  "executionMode": "FG",
  "tasks": [
    {
      "name": "t1",
      "pageSize": 20,
      "reader": {
        "type": "sqlreader",
        "dbFile": "${params.dbFile}",
        "sqlQuery": "select * from ${params.tableName}"
      },
      "writer": {
        "type": "csvWriter"
      }
    }
  ]
}