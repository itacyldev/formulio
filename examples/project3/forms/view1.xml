<main repo="provRepo">
    <list name="My">
        <button>
            <action type="js">
                <param name="method"
                    value="vh.setWidgetValue('myInput',123.5)" />
            </action>
        </button>
        <button label="button2">
            <action type="js" method="session.set('miProp',(session.get('miProp') == null)?1:session.get('miProp')+1)" />
        </button>
        <input id="myInput" value="${session.miProp}" />
        <datatable repo="contacts" />
    </list>
</main>
