<main>
    <view name="My">
        <button label="jsAction">
            <action method="vh.setWidgetValue('myInput',123.5);console.log(vh.widget('myInput'))"
                type="js" />
        </button>
        <button label="run Job">
            <action>
                <action registerInHistory="false" type="job">
                    <param name="jobId" value="cartodruid_sync_job_cmd" />
                </action>
                <nav route="form7-list"/>
            </action>
        </button>
        <input id="myInput" value="you are in view 1" />
        <datatable repo="data1" />
        <buttonbar type="fab">
            <button action="create" route="form7-edit1">
                <param name="repo" value="data1" />
                <param name="f_created" value="${date.now}" />
                <param name="f_updated" value="${date.now}" />
                <param name="name" value="${random:string()}" />
                <param name="age" value="${random:integer(0,50)}" />
            </button>
        </buttonbar>
    </view>
</main>
