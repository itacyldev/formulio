<main name="basicForm" description="Basic CRUD" id="form5" repo="contacts">
    <list name="Basic Form">
        <datatable route="form5-edit1" />
    </list>
    <edit>
        <form>
            <input id="fn" validator="required" value="${entity.first_name}" />
            <input validator="required" value="${entity.bio}" render="${view.fn.length() > 3}" />
        </form>
    </edit>
</main>
