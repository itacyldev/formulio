<main id="deposito" name="Depósito" repo="depositoRepo" mainForm="false">
    <list id="listDeposito" name="Depósito" description="Listado depósitos">
       <datatable>
           <column id="column_numero" headerText="Número" filtering="true" ordering="true" value="${entity.numero}">
               <filter property="numero" matching="contains" valueExpression="${this.column_numero}"/>
               <order property="numero"/>
           </column>
           <column id="column_razon_social" headerText="Razón social" filtering="false" ordering="false" value="${entity.operador.razon_social}"/>
           <column id="column_material" headerText="Material" filtering="true" ordering="true" value="${entity.material}">
               <filter property="material" matching="contains" valueExpression="${this.column_material}"/>
               <order property="material"/>
           </column>
           <column id="column_capacidad_deposito" headerText="Capacidad (litros)" filtering="true" ordering="true" value="${entity.capacidad}">
               <filter property="capacidad" matching="contains" valueExpression="${this.column_capacidad_deposito}"/>
               <order property="capacidad"/>
           </column>
       </datatable>
        <buttonbar type="fab"/>
    </list>
    <edit id="editDeposito">
        <form id="formDeposito">

            <p value=""/>
            <p backgroundColor="#26a69a" fontSize="22" fontColor="#FFFFFF" bold="true" value="DEPÓSITO"/>
            <p value=""/>

            <p value=""/>
            <divisor color="#00766C" strokeWidth="10" />
            <p bold="true" value="Operador:" fontSize="18"/>
            <divisor color="#00766C" strokeWidth="10" />

            <table border="false">
                <row>
                    <table headerText=" Id, Nombre operador" weights="20, 80">
                        <row>
                            <input value="${entity.operador_id}" placeHolder="${not empty(params.operador_id)?params.operador_id:not empty(params.entityId)?params.entityId:null}" readonly="true" />
                            <textarea value="${entity.operador.razon_social}" placeHolder="${not empty(params.razon_social)?params.razon_social:null}" readonly="true" />
                        </row>
                    </table>
                </row>
            </table>

            <p value=""/>
            <divisor color="#00766C" strokeWidth="10" />
            <p bold="true" value="Depósito:" fontSize="18"/>
            <divisor color="#00766C" strokeWidth="10" />

            <input label="Número: " value="${entity.numero}" />

            <input label="Material: " value="${entity.material}"  />

            <input label="Capacidad (litros): " value="${entity.capacidad}" inputType="3"/>

        </form>

        <buttonbar type="bottom">
            <button label="Save">
                <action type="save" route="back" registerInHistory="false" restoreView="true">
                    <param name="entityId" value="${not empty(params.expediente_id)?params.expediente_id:not empty(params.granel_deposito_id)?params.granel_deposito_id:params.operador_id}" />
                </action>
            </button>

            <button label="Cancel">
                <action type="cancel" route="back" restoreView="true">
                    <param name="entityId" value="${not empty(params.expediente_id)?params.expediente_id:not empty(params.granel_deposito_id)?params.granel_deposito_id:params.operador_id}" />
                </action>
            </button>

            <button label="Delete" readonly="${empty (entity.deposito_id)}" readonlyMessage="No puede realizar esta acción: No existe el depósito" restoreView="true"  confirmation="true" labelConfirmation="Se va a eliminar el depósito. ¿Desea continuar?">
                <action type="delete" route="back" registerInHistory="false">
                    <param name="entityId" value="${not empty(params.expediente_id)?params.expediente_id:not empty(params.granel_deposito_id)?params.granel_deposito_id:params.operador_id}" />
                </action>
            </button>
        </buttonbar>
    </edit>
</main>
