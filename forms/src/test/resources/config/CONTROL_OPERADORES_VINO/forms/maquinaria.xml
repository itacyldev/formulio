<main id="maquinaria" name="Maquinaria" repo="maquinariaRepo" mainForm="false">
    <list id="listMaquinaria" name="Maquinaria" description="Listado maquinaria">
       <datatable>
           <column id="column_tipo" headerText="Tipo" filtering="true" ordering="true" value="${entity.tipo}">
               <filter property="tipo" matching="contains" valueExpression="${this.column_tipo}"/>
               <order property="tipo"/>
           </column>
           <column id="column_razon_social" headerText="Razón social" filtering="false" ordering="false" value="${entity.operador.razon_social}"/>
           <column id="column_rto" headerText="Rto" filtering="true" ordering="true" value="${entity.rto}">
               <filter property="rto" matching="contains" valueExpression="${this.column_rto}"/>
               <order property="rto"/>
           </column>
           <column id="column_capacidad" headerText="Capacidad" filtering="true" ordering="true" value="${entity.capacidad}">
               <filter property="capacidad" matching="contains" valueExpression="${this.column_capacidad}"/>
               <order property="capacidad"/>
           </column>
           <column id="column_marca" headerText="Marca" filtering="true" ordering="true" value="${entity.marca}">
               <filter property="marca" matching="contains" valueExpression="${this.column_marca}"/>
               <order property="marca"/>
           </column>
           <column id="column_modelo" headerText="Modelo" filtering="true" ordering="true" value="${entity.modelo}">
               <filter property="modelo" matching="contains" valueExpression="${this.column_modelo}"/>
               <order property="modelo"/>
           </column>
           <column id="column_observaciones" headerText="Observaciones" filtering="true" ordering="true" value="${entity.observaciones}">
               <filter property="observaciones" matching="contains" valueExpression="${this.column_observaciones}"/>
               <order property="observaciones"/>
           </column>
       </datatable>
        <buttonbar type="fab"/>
    </list>
    <edit id="editMaquinaria">
        <form id="formMaquinaria">

            <p value=""/>
            <p backgroundColor="#26a69a" fontSize="22" fontColor="#FFFFFF" bold="true" value="MAQUINARIA"/>
            <p value=""/>

            <p value=""/>
            <divisor color="#00766C" strokeWidth="10" />
            <p bold="true" value="Operador:" fontSize="18"/>
            <divisor color="#00766C" strokeWidth="10" />

            <table border="false">
                <row>
                    <table headerText=" Id, Nombre operador" weights="20, 80">
                        <row>
                            <input value="${entity.operador_id}" placeHolder="${not empty(params.operador_id)?params.operador_id:not empty(params.entityId)?params.entityId:null}" readonly="true" />
                            <textarea value="${entity.operador.razon_social}" placeHolder="${not empty(params.razon_social)?params.razon_social:null}" readonly="true" />
                        </row>
                    </table>
                </row>
            </table>


            <p value=""/>
            <divisor color="#00766C" strokeWidth="10" />
            <p bold="true" value="Maquinaria:" fontSize="18"/>
            <divisor color="#00766C" strokeWidth="10" />

            <input label="Tipo: " value="${entity.tipo}"  />

            <input label="Rto: " value="${entity.rto}"  />

            <input label="Capacidad: " value="${entity.capacidad}"  />

            <input label="Marca: " value="${entity.marca}"  />

            <input label="Modelo: " value="${entity.modelo}"  />

            <textarea label="Observaciones: " value="${entity.observaciones}"  />

        </form>

        <buttonbar type="bottom">
            <button label="Save">
                <action type="save" route="back" registerInHistory="false" restoreView="true">
                    <param name="entityId" value="${not empty(params.expediente_id)?params.expediente_id:params.operador_id}" />
                </action>
            </button>

            <button label="Cancel">
                <action type="cancel" route="back" restoreView="true">
                    <param name="entityId" value="${not empty(params.expediente_id)?params.expediente_id:params.operador_id}" />
                </action>
            </button>

            <button label="Delete" readonly="${empty (entity.maquinaria_id)}" readonlyMessage="No puede realizar esta acción: No existe la maquinaria" restoreView="true"  confirmation="true" labelConfirmation="Se va a eliminar la maquinaria. ¿Desea continuar?">
                <action type="delete" route="back" registerInHistory="false">
                    <param name="entityId" value="${not empty(params.expediente_id)?params.expediente_id:params.operador_id}" />
                </action>
            </button>
        </buttonbar>
    </edit>
</main>
