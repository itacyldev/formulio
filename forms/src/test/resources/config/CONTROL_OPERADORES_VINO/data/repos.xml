<repos>
    <repo dbFile="controloperadoresvino.sqlite" id="provRepo" dbTable="comu_provincia" />
    <repo dbFile="controloperadoresvino.sqlite" id="muniRepo" dbTable="comu_municipio_21">
        <mapping fk="c_provincia" property="provincia" repo="provRepo"/>
    </repo>
    <repo dbFile="controloperadoresvino.sqlite" id="locaRepo" dbTable="comu_localidad_21">
        <mapping fk="c_provincia" property="provincia" repo="provRepo"/>
        <mapping fk="c_municipio_id" property="municipio" repo="muniRepo"/>
    </repo>

    <repo dbFile="controloperadoresvino.sqlite" id="dopRepo" dbTable="dop"/>

    <repo dbFile="controloperadoresvino.sqlite" id="provinciaProduccionRepo" dbTable="provincia_produccion">
        <mapping fk="dop_id" property="dop" repo="dopRepo"/>
    </repo>

    <repo dbFile="controloperadoresvino.sqlite" id="vinoAptitudRepo" dbTable="vino_aptitud">
        <mapping fk="dop_id" property="dop" repo="dopRepo"/>
    </repo>

    <repo dbFile="controloperadoresvino.sqlite" id="municipioProduccionRepo" dbTable="municipio_produccion">
        <mapping fk="provincia_produccion_id" property="provinciaProduccion" repo="provinciaProduccionRepo"/>
    </repo>

    <repo dbFile="controloperadoresvino.sqlite" id="operadorRepo" dbTable="operador">
       <meta>
            <keyGenerator type="numericUUID"/>
            <property name="f_actualizacion" converter="date" pattern="seconds" evalOn="update" expression="${date.now}" />
        </meta>
        <mapping fk="dop_id" property="dop" repo="dopRepo"/>
    </repo>

    <repo dbFile="controloperadoresvino.sqlite" id="expedienteRepo" dbTable="expediente">
       <meta>
            <keyGenerator type="numericUUID"/>
            <property name="f_actualizacion" converter="date" pattern="seconds" evalOn="update" expression="${date.now}" />
        </meta>
        <mapping fk="dop_id" property="dop" repo="dopRepo"/>
        <mapping fk="operador_id" property="operador" repo="operadorRepo"/>
    </repo>

    <repo dbFile="controloperadoresvino.sqlite" id="maquinariaRepo" dbTable="maquinaria">
        <meta>
            <keyGenerator type="numericUUID"/>
            <property name="f_actualizacion" converter="date" pattern="seconds" evalOn="update" expression="${date.now}" />
        </meta>
        <mapping fk="operador_id" property="operador" repo="operadorRepo"/>
    </repo>

    <repo dbFile="controloperadoresvino.sqlite" id="depositoRepo" dbTable="deposito">
        <meta>
            <keyGenerator type="numericUUID"/>
            <property name="f_actualizacion" converter="date" pattern="seconds" evalOn="update" expression="${date.now}" />
        </meta>
        <mapping fk="operador_id" property="operador" repo="operadorRepo"/>
    </repo>

    <repo dbFile="controloperadoresvino.sqlite" id="barricaRepo" dbTable="barrica">
        <meta>
            <keyGenerator type="numericUUID"/>
            <property name="f_actualizacion" converter="date" pattern="seconds" evalOn="update" expression="${date.now}" />
        </meta>
        <mapping fk="operador_id" property="operador" repo="operadorRepo"/>
    </repo>


    <repo dbFile="controloperadoresvino.sqlite" id="preguntaRepo" dbTable="pregunta">
        <meta>
            <property name="f_actualizacion" converter="date" pattern="seconds" evalOn="update" expression="${date.now}" />
        </meta>
    </repo>

    <repo dbFile="controloperadoresvino.sqlite" id="documentacionRegistroRepo" dbTable="documentacion_registro">
         <mapping fk="expediente_id" property="expediente" repo="expedienteRepo"/>
        <mapping fk="pregunta_id" property="pregunta" repo="preguntaRepo"/>
    </repo>

    <repo dbFile="controloperadoresvino.sqlite" id="tipoVinoRepo" dbTable="tipo_vino">
        <mapping fk="expediente_id" property="expediente" repo="expedienteRepo"/>
        <mapping fk="pregunta_id" property="pregunta" repo="preguntaRepo"/>
    </repo>

    <repo dbFile="controloperadoresvino.sqlite" id="controlDocumentalLibroRegistroRepo" dbTable="control_documental_libro_registro">
       <mapping fk="expediente_id" property="expediente" repo="expedienteRepo"/>
        <mapping fk="pregunta_id" property="pregunta" repo="preguntaRepo"/>
    </repo>

    <repo dbFile="controloperadoresvino.sqlite" id="controlDocumentalDeclaracionesObligatoriasRepo" dbTable="control_documental_declaraciones_obligatorias">
        <mapping fk="expediente_id" property="expediente" repo="expedienteRepo"/>
        <mapping fk="pregunta_id" property="pregunta" repo="preguntaRepo"/>
    </repo>

    <repo dbFile="controloperadoresvino.sqlite" id="controlDocumentalDocumentosAcompanamientoRepo" dbTable="control_documental_documentos_acompanamiento">
         <mapping fk="expediente_id" property="expediente" repo="expedienteRepo"/>
        <mapping fk="pregunta_id" property="pregunta" repo="preguntaRepo"/>
    </repo>

    <repo dbFile="controloperadoresvino.sqlite" id="revisionDocumentalRepo" dbTable="revision_documental">
         <mapping fk="expediente_id" property="expediente" repo="expedienteRepo"/>
        <mapping fk="pregunta_id" property="pregunta" repo="preguntaRepo"/>
    </repo>

    <repo dbFile="controloperadoresvino.sqlite" id="verificacionInstalacionRepo" dbTable="verificacion_instalacion">
         <mapping fk="expediente_id" property="expediente" repo="expedienteRepo"/>
        <mapping fk="pregunta_id" property="pregunta" repo="preguntaRepo"/>
    </repo>

    <repo dbFile="controloperadoresvino.sqlite" id="verificacionPliegoCondicionesRepo" dbTable="verificacion_pliego_condiciones">
          <mapping fk="expediente_id" property="expediente" repo="expedienteRepo"/>
        <mapping fk="pregunta_id" property="pregunta" repo="preguntaRepo"/>
    </repo>

    <repo dbFile="controloperadoresvino.sqlite" id="tipoEnvejecimientoRepo" dbTable="tipo_envejecimiento"/>

    <repo dbFile="controloperadoresvino.sqlite" id="trazabilidadRepo" dbTable="trazabilidad">
       <meta>
            <keyGenerator type="numericUUID"/>
            <property name="f_actualizacion" converter="date" pattern="seconds" evalOn="update" expression="${date.now}" />
        </meta>
        <mapping fk="expediente_id" property="expediente" repo="expedienteRepo"/>
        <mapping fk="tipo_envejecimiento_id" property="tipoEnvejecimiento" repo="tipoEnvejecimientoRepo"/>
    </repo>

    <repo dbFile="controloperadoresvino.sqlite" id="trazabilidadBarricaRepo" dbTable="trazabilidad_barrica">
        <meta>
            <keyGenerator type="numericUUID"/>
            <property name="f_actualizacion" converter="date" pattern="seconds" evalOn="update" expression="${date.now}" />
        </meta>
        <mapping fk="trazabilidad_id" property="trazabilidad" repo="trazabilidadRepo"/>
    </repo>

    <repo dbFile="controloperadoresvino.sqlite" id="variedadRepo" dbTable="variedad">
    </repo>

    <repo dbFile="controloperadoresvino.sqlite" id="tipoUvaRepo" dbTable="tipo_uva">
        <mapping fk="dop_id" property="dop" repo="dopRepo"/>
        <mapping fk="variedad_id" property="variedad" repo="variedadRepo"/>
    </repo>

    <repo dbFile="controloperadoresvino.sqlite" id="entradaUvaRepo" dbTable="entrada_uva">
       <meta>
            <keyGenerator type="numericUUID"/>
            <property name="f_actualizacion" converter="date" pattern="seconds" evalOn="update" expression="${date.now}" />
        </meta>
        <mapping fk="trazabilidad_id" property="trazabilidad" repo="trazabilidadRepo"/>
        <mapping fk="provincia_produccion_id" property="provinciaProduccion" repo="provinciaProduccionRepo"/>
        <mapping fk="municipio_produccion_id" property="municipioProduccion" repo="municipioProduccionRepo"/>
        <mapping fk="variedad_id" property="variedad" repo="variedadRepo"/>
        <mapping fk="tipo_uva_id" property="tipoUva" repo="tipoUvaRepo"/>
        <mapping fk="deposito_id" property="deposito" repo="depositoRepo"/>
    </repo>

    <repo dbFile="controloperadoresvino.sqlite" id="loteRepo" dbTable="lote">
       <meta>
            <keyGenerator type="numericUUID"/>
            <property name="f_actualizacion" converter="date" pattern="seconds" evalOn="update" expression="${date.now}" />
        </meta>
        <mapping fk="expediente_id" property="expediente" repo="expedienteRepo"/>
    </repo>

    <repo dbFile="controloperadoresvino.sqlite" id="vinoRepo" dbTable="vino"/>
    <repo dbFile="controloperadoresvino.sqlite" id="nivelProteccionRepo" dbTable="nivel_proteccion"/>
    <repo dbFile="controloperadoresvino.sqlite" id="tipoDepositoRepo" dbTable="tipo_deposito"/>
    <repo dbFile="controloperadoresvino.sqlite" id="materialRepo" dbTable="material"/>

    <repo dbFile="controloperadoresvino.sqlite" id="partidaAforoRepo" dbTable="partida_aforo">
       <meta>
            <keyGenerator type="numericUUID"/>
            <property name="f_actualizacion" converter="date" pattern="seconds" evalOn="update" expression="${date.now}" />
        </meta>
        <mapping fk="expediente_id" property="expediente" repo="expedienteRepo"/>
        <mapping fk="vino_id" property="vino" repo="vinoRepo"/>
        <mapping fk="nivel_proteccion_id" property="nivelProteccion" repo="nivelProteccionRepo"/>
    </repo>

    <repo dbFile="controloperadoresvino.sqlite" id="granelDepositoRepo" dbTable="granel_deposito">
       <meta>
            <keyGenerator type="numericUUID"/>
           <property name="f_actualizacion" converter="date" pattern="seconds" evalOn="update" expression="${date.now}" />
        </meta>
        <mapping fk="partida_aforo_id" property="partidaAforo" repo="partidaAforoRepo"/>
        <mapping fk="tipo_deposito_id" property="tipoDeposito" repo="tipoDepositoRepo"/>
        <mapping fk="material_id" property="material" repo="materialRepo"/>
        <mapping fk="deposito_id" property="deposito" repo="depositoRepo"/>
    </repo>

    <repo dbFile="controloperadoresvino.sqlite" id="granelBarricaRepo" dbTable="granel_barrica">
       <meta>
            <keyGenerator type="numericUUID"/>
            <property name="f_actualizacion" converter="date" pattern="seconds" evalOn="update" expression="${date.now}" />
        </meta>
        <mapping fk="partida_aforo_id" property="partidaAforo" repo="partidaAforoRepo"/>
        <mapping fk="barrica_id" property="barrica" repo="barricaRepo"/>
    </repo>

    <repo dbFile="controloperadoresvino.sqlite" id="envasadoRepo" dbTable="envasado">
       <meta>
            <keyGenerator type="numericUUID"/>
            <property name="f_actualizacion" converter="date" pattern="seconds" evalOn="update" expression="${date.now}" />
        </meta>
        <mapping fk="partida_aforo_id" property="partidaAforo" repo="partidaAforoRepo"/>
    </repo>

    <repo dbFile="controloperadoresvino.sqlite" id="etiquetadoRepo" dbTable="etiquetado">
       <meta>
            <keyGenerator type="numericUUID"/>
           <property name="f_actualizacion" converter="date" pattern="seconds" evalOn="update" expression="${date.now}" />
        </meta>
        <mapping fk="partida_aforo_id" property="partidaAforo" repo="partidaAforoRepo"/>
    </repo>

    <repo dbFile="controloperadoresvino.sqlite" id="expedienteVinoRepo" dbTable="expediente_vino">
       <meta>
            <keyGenerator type="numericUUID"/>
           <property name="f_actualizacion" converter="date" pattern="seconds" evalOn="update" expression="${date.now}" />
        </meta>
        <mapping fk="expediente_id" property="expediente" repo="expedienteRepo"/>
    </repo>

    <repo dbFile="controloperadoresvino.sqlite" id="expedienteNivelProteccionRepo" dbTable="expediente_nivel_proteccion">
       <meta>
            <keyGenerator type="numericUUID"/>
           <property name="f_actualizacion" converter="date" pattern="seconds" evalOn="update" expression="${date.now}" />
        </meta>
        <mapping fk="expediente_id" property="expediente" repo="expedienteRepo"/>
    </repo>

    <repo dbFile="controloperadoresvino.sqlite" id="desviacionRepo" dbTable="desviacion">
       <mapping fk="expediente_id" property="expediente" repo="expedienteRepo"/>
     </repo>

    <repo dbFile="controloperadoresvino.sqlite" id="actaTomaMuestraRepo" dbTable="acta_toma_muestra">
       <meta>
            <keyGenerator type="numericUUID"/>
           <property name="f_actualizacion" converter="date" pattern="seconds" evalOn="update" expression="${date.now}" />
        </meta>
        <mapping fk="expediente_id" property="expediente" repo="expedienteRepo"/>
    </repo>

    <repo dbFile="controloperadoresvino.sqlite" id="muestraRepo" dbTable="muestra">
       <meta>
            <keyGenerator type="numericUUID"/>
           <property name="f_actualizacion" converter="date" pattern="seconds" evalOn="update" expression="${date.now}" />
        </meta>
        <mapping fk="acta_id" property="acta" repo="actaTomaMuestraRepo"/>
    </repo>

    <repo dbFile="controloperadoresvino.sqlite" id="numeracionContraetiquetadoRepo" dbTable="numeracion_contraetiquetado">
       <meta>
            <keyGenerator type="numericUUID"/>
           <property name="f_actualizacion" converter="date" pattern="seconds" evalOn="update" expression="${date.now}" />
        </meta>
        <mapping fk="trazabilidad_id" property="trazabilidad" repo="trazabilidadRepo"/>
    </repo>

</repos>