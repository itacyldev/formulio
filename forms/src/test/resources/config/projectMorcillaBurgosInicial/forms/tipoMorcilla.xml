<main id="tipoMorcilla" name="Tipo de morcilla" repo="tipoMorcillaRepo">
    <list id="listTipoMorcilla" name="Tipo de morcilla" description="Listado tipos de morcilla">
       <!--<datatable properties="tipo_morcilla, expediente.num_expediente, expediente.operador.razon_social, peso_unitario_kg" />-->
        <datatable>
            <column id="column_tipo_morcilla" headerText="Tipo morcilla" filtering="true" ordering="true" value="${entity.tipo_morcilla}">
                <filter property="tipo_morcilla" matching="contains" valueExpression="${this.column_tipo_morcilla}"/>
                <order property="tipo_morcilla"/>
            </column>
            <column id="column_num_expediente" headerText="Nº Exp." filtering="false" ordering="false" value="${entity.expediente.num_expediente}"/>
            <column id="column_razon_social" headerText="Razón social" filtering="false" ordering="false" value="${entity.expediente.operador.razon_social}"/>

            <column id="column_peso_unitario_kg" headerText="Peso unitario Kg" filtering="true" ordering="true" value="${entity.peso_unitario_kg}">
                <filter property="peso_unitario_kg" matching="contains" valueExpression="${this.column_peso_unitario_kg}"/>
                <order property="peso_unitario_kg"/>
            </column>
        </datatable>
        <buttonbar type="fab"/>
    </list>
    <edit id="editTipoMorcilla">
        <form id="formTipoMorcilla">

            <p value=""/>
            <divisor color="#00766C" strokeWidth="8" />
            <p bold="true" value="EXPEDIENTE: "/>
            <divisor color="#00766C" strokeWidth="8" />

            <table border="false">
                <row>
                    <table headerText=" Id. Exp., Nº Expediente, Operador" weights="25, 35, 40" >
                        <row>
                            <input value="${entity.expediente_id}" placeHolder="${not empty(params.expediente_id)?params.expediente_id:null}" readonly="true"/>
                            <textarea value="${entity.expediente.num_expediente}" placeHolder="${not empty(params.num_expediente)?params.num_expediente:null}" readonly="true" />
                            <textarea value="${entity.expediente.operador.razon_social}" placeHolder="${not empty(params.operador)?params.operador:null}" readonly="true" />
                        </row>
                    </table>
                </row>
            </table>

            <p value=""/>
            <divisor color="#00766C" strokeWidth="8" />
            <p bold="true" value="TIPO DE MORCILLA: "/>
            <divisor color="#00766C" strokeWidth="8" />

            <input label="4.2.1 Tipo de morcilla: " value="${entity.tipo_morcilla}" validator="required"/>

            <input id="peso_unitario_kg" label="4.2.2 Peso unitario (kg.): " value="${entity.peso_unitario_kg}" inputType="3"/>

            <input label="4.2.3 Unidades teóricas por kg.: " value="${view.peso_unitario_kg gt 0? math:round(math:pow(10,2)/view.peso_unitario_kg)/math:pow(10,2):null}" inputType="3" readonly="true"/>

        </form>
        <!--<actions>
            <save label="Save" route="${not empty (params.backRoute)? params.backRoute : 'tipoMorcilla-listTipoMorcilla'}" registerInHistory="false" restoreView="true">
                <param name="entityId" value="${params.expediente_id}" />
            </save>
            <cancel label="Cancel" route="${not empty (params.backRoute)? params.backRoute : 'tipoMorcilla-listTipoMorcilla'}" restoreView="true">
                <param name="entityId" value="${params.expediente_id}" />
            </cancel>
            <delete label="Delete" route="${empty (entity.tipo_morcilla_id) ? null : (not empty (params.backRoute)? params.backRoute : 'tipoMorcilla-listTipoMorcilla')}" registerInHistory="false" message="No puede realizar esta acción: No existe tipo de morcilla" restoreView="true">
                <param name="entityId" value="${params.expediente_id}" />
            </delete>
        </actions>-->

        <buttonbar type="bottom">
            <button label="Save">
                <action type="save" route="back" registerInHistory="false" restoreView="true">
                    <param name="entityId" value="${params.expediente_id}" />
                </action>
            </button>

            <button label="Cancel">
                <action type="cancel" route="back" restoreView="true">
                    <param name="entityId" value="${params.expediente_id}" />
                </action>
            </button>

            <button label="Delete" readonly="${empty (entity.tipo_morcilla_id)}" readonlyMessage="No puede realizar esta acción: No existe tipo de morcilla">
                <action type="delete" route="back" registerInHistory="false" restoreView="true">
                    <param name="entityId" value="${params.expediente_id}" />
                </action>
            </button>
        </buttonbar>
    </edit>
</main>
