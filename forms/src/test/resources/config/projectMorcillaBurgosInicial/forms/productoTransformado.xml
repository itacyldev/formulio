<main id="prodTransformado" name="Producto transformado" repo="productoTransformadoRepo">
    <list id="listTransformado" name="Producto transformado" description="Listado productos transformados">
       <!--<datatable properties="num_lote, expediente.num_expediente, expediente.operador.razon_social, presentacion, ingredientes, cumple_producto_transformado, notas_aclaratorias" />-->
        <datatable>
            <column id="column_num_lote" headerText="Nº Lote" filtering="true" ordering="true" value="${entity.num_lote}">
                <filter property="num_lote" matching="contains" valueExpression="${this.column_num_lote}"/>
                <order property="num_lote"/>
            </column>
            <column id="column_num_expediente" headerText="Nº Exp." filtering="false" ordering="false" value="${entity.expediente.num_expediente}"/>
            <column id="column_razon_social" headerText="Razón social" filtering="false" ordering="false" value="${entity.expediente.operador.razon_social}"/>
            <column id="column_presentacion" headerText="Presentación" filtering="true" ordering="true" value="${entity.presentacion}">
                <filter property="presentacion" matching="contains" valueExpression="${this.column_presentacion}"/>
                <order property="presentacion"/>
            </column>
            <column id="column_ingredientes" headerText="Ingredientes" filtering="true" ordering="true" value="${entity.ingredientes}">
                <filter property="ingredientes" matching="contains" valueExpression="${this.column_ingredientes}"/>
                <order property="ingredientes"/>
            </column>
            <column id="column_cumple_producto_transformado" headerText="Cumple producto transformado" filtering="true" ordering="true" value="${entity.cumple_producto_transformado}">
                <filter property="cumple_producto_transformado" matching="contains" valueExpression="${this.column_cumple_producto_transformado}"/>
                <order property="cumple_producto_transformado"/>
            </column>
            <column id="column_notas_aclaratorias" headerText="Notas aclaratorias" filtering="true" ordering="true" value="${entity.notas_aclaratorias}">
                <filter property="notas_aclaratorias" matching="contains" valueExpression="${this.column_notas_aclaratorias}"/>
                <order property="notas_aclaratorias"/>
            </column>
        </datatable>
        <buttonbar type="fab"/>
    </list>
    <edit id="editProdTransformado">
        <form id="formProductoTransformado">

            <p value=""/>
            <divisor color="#00766C" strokeWidth="8" />
            <p bold="true" value="EXPEDIENTE: "/>
            <divisor color="#00766C" strokeWidth="8" />

            <table border="false">
                <row>
                    <table headerText=" Id. Exp., Nº Expediente, Operador" weights="25, 35, 40" >
                        <row>
                            <input value="${entity.expediente_id}" placeHolder="${not empty(params.expediente_id)?params.expediente_id:null}" readonly="true"/>
                            <textarea value="${entity.expediente.num_expediente}" placeHolder="${not empty(params.num_expediente)?params.num_expediente:null}" readonly="true" />
                            <textarea value="${entity.expediente.operador.razon_social}" placeHolder="${not empty(params.operador)?params.operador:null}" readonly="true" />
                        </row>
                    </table>
                </row>
            </table>

            <p value=""/>
            <divisor color="#00766C" strokeWidth="8" />
            <p bold="true" value="LOTE: "/>
            <divisor color="#00766C" strokeWidth="8" />

            <input label="5.1 Nº Lote: " value="${entity.num_lote}" validator="required"/>

            <input label="5.2 Presentación/Delimitación: " value="${entity.presentacion}" />

            <textarea label="5.3 Ingredientes: " value="${entity.ingredientes}" />

            <radio label="5.4 Cumple: " value="${entity.cumple_producto_transformado}" orientation="horizontal" weights="15, 85">
                <options>
                    <option label="Sí" value="S" />
                    <option label="No" value="N" />
                </options>
            </radio>

            <textarea label="5.5 Notas aclaratorias: " value="${entity.notas_aclaratorias}" />
        </form>
        <!--<actions>
            <save label="Save" route="${not empty (params.backRoute)? params.backRoute : 'prodTransformado-listTransformado'}" registerInHistory="false" restoreView="true">
                <param name="entityId" value="${params.expediente_id}" />
            </save>
            <cancel label="Cancel" route="${not empty (params.backRoute)? params.backRoute : 'prodTransformado-listTransformado'}" restoreView="true">
                <param name="entityId" value="${params.expediente_id}" />
            </cancel>
            <delete label="Delete" route="${empty (entity.producto_transformado_id) ? null : (not empty (params.backRoute)? params.backRoute : 'prodTransformado-listTransformado')}" registerInHistory="false" message="No puede realizar esta acción: No existe lote producto transformado" restoreView="true">
                <param name="entityId" value="${params.expediente_id}" />
            </delete>
        </actions>-->

        <buttonbar type="bottom">
            <button label="Save">
                <action type="save" route="back" registerInHistory="false" restoreView="true">
                    <param name="entityId" value="${params.expediente_id}" />
                </action>
            </button>

            <button label="Cancel">
                <action type="cancel" route="back" restoreView="true">
                    <param name="entityId" value="${params.expediente_id}" />
                </action>
            </button>

            <button label="Delete" readonly="${empty (entity.producto_transformado_id)}" readonlyMessage="No puede realizar esta acción: No existe lote producto transformado">
                <action type="delete" route="back" registerInHistory="false" restoreView="true">
                    <param name="entityId" value="${params.expediente_id}" />
                </action>
            </button>
        </buttonbar>

    </edit>
</main>
