<main id="barrica" name="Barrica" repo="barricaRepo" mainForm="false">
    <list id="listBarrica" name="Barrica" description="Listado barricas">
       <datatable>
           <column id="column_unidades" headerText="Unidades" filtering="true" ordering="true" value="${entity.unidades}">
               <filter property="unidades" matching="contains" valueExpression="${this.column_unidades}"/>
               <order property="unidades"/>
           </column>
           <column id="column_razon_social" headerText="Razón social" filtering="false" ordering="false" value="${entity.operador.razon_social}"/>
           <column id="column_tipo_roble" headerText="Tipo roble" filtering="true" ordering="true" value="${entity.tipo_roble}">
               <filter property="tipo_roble" matching="contains" valueExpression="${this.column_tipo_roble}"/>
               <order property="tipo_roble"/>
           </column>
           <column id="column_numeracion" headerText="Numeración" filtering="true" ordering="true" value="${entity.numeracion}">
               <filter property="numeracion" matching="contains" valueExpression="${this.column_numeracion}"/>
               <order property="numeracion"/>
           </column>
           <column id="column_cap_unit" headerText="Cap. Unit." filtering="true" ordering="true" value="${entity.cap_unit}">
               <filter property="cap_unit" matching="contains" valueExpression="${this.column_cap_unit}"/>
               <order property="cap_unit"/>
           </column>
           <column id="column_cap_total" headerText="Cap. Total" filtering="true" ordering="true" value="${entity.cap_total}">
               <filter property="cap_total" matching="contains" valueExpression="${this.column_cap_total}"/>
               <order property="cap_total"/>
           </column>
           <column id="column_edad" headerText="Edad" filtering="true" ordering="true" value="${entity.edad}">
               <filter property="edad" matching="contains" valueExpression="${this.column_edad}"/>
               <order property="edad"/>
           </column>
       </datatable>
        <buttonbar type="fab"/>
    </list>
    <edit id="editBarrica">
        <form id="formBarrica">

            <p value=""/>
            <p backgroundColor="#26a69a" fontSize="22" fontColor="#FFFFFF" bold="true" value="BARRICA"/>
            <p value=""/>

            <p value=""/>
            <divisor color="#00766C" strokeWidth="10" />
            <p bold="true" value="Operador:" fontSize="18"/>
            <divisor color="#00766C" strokeWidth="10" />

            <table border="false">
                <row>
                    <table headerText=" Id, Nombre operador" weights="20, 80">
                        <row>
                            <input value="${entity.operador_id}" placeHolder="${not empty(params.operador_id)?params.operador_id:not empty(params.entityId)?params.entityId:null}" readonly="true" />
                            <textarea value="${entity.operador.razon_social}" placeHolder="${not empty(params.razon_social)?params.razon_social:null}" readonly="true" />
                        </row>
                    </table>
                </row>
            </table>

            <p value=""/>
            <divisor color="#00766C" strokeWidth="10" />
            <p bold="true" value="Barrica:" fontSize="18"/>
            <divisor color="#00766C" strokeWidth="10" />

            <input label="Unidades: " value="${entity.unidades}" inputType="3"  />

            <input label="Tipo roble: " value="${entity.tipo_roble}"  />

            <input label="Numeración: " value="${entity.numeracion}"  />

            <input label="Cap. Unit.: " value="${entity.cap_unit}" inputType="3"/>

            <input label="Cap. Total: " value="${entity.cap_total}" inputType="3"/>

            <input label="Edad: " value="${entity.edad}" inputType="3"/>

        </form>

        <buttonbar type="bottom">
            <button label="Save">
                <action type="save" route="back" registerInHistory="false" restoreView="true">
                    <param name="entityId" value="${not empty(params.expediente_id)?params.expediente_id:params.operador_id}" />
                </action>
            </button>

            <button label="Cancel">
                <action type="cancel" route="back" restoreView="true">
                    <param name="entityId" value="${not empty(params.expediente_id)?params.expediente_id:params.operador_id}" />
                </action>
            </button>

            <button label="Delete" readonly="${empty (entity.barrica_id)}" readonlyMessage="No puede realizar esta acción: No existe el depósito" restoreView="true">
                <action type="delete" route="back" registerInHistory="false">
                    <param name="entityId" value="${not empty(params.expediente_id)?params.expediente_id:params.operador_id}" />
                </action>
            </button>
        </buttonbar>
    </edit>
</main>
