<main name="COMPONENTS 2" id="formComponents2" repo="prue">
    <edit>
        <form>
            <p value=""/>
            <p backgroundColor="#26a69a" fontSize="22" fontColor="#FFFFFF" bold="true" value="FORM"/>
            <p value=""/>

            <datatable id="datatableProvincia2" repo="provRepo" numVisibleRows="5">
                <column id="column_c_provincia_id" headerText="Cod. Provincia" filtering="true" ordering="true" value="${entity.id}">
                    <filter property="id" matching="contains" valueExpression="${this.column_c_provincia_id}"/>
                    <order property="id"/>
                </column>
                <column id="column_d_provincia" headerText="Provincia" filtering="true" ordering="true" value="${entity.name}">
                    <filter property="name" matching="contains" valueExpression="${this.column_d_provincia}"/>
                    <order property="name"/>
                </column>
            </datatable>

            <card label="Address" expandable="true" expanded="true" imagePosition="left">
                <p name="title" value="123 Main Street, Anytown"/>
                <p name="subtitle" value="United States"/>
            </card>

            <tab>
                <tabitem label="User data">
                </tabitem>
                <tabitem label="Address">
                </tabitem>
                <tabitem label="Additional information">
                </tabitem>

            </tab>

        </form>
    </edit>
    <list name="COMPONENTS 2">
        <datatable properties="input, radio, textarea, switcher, fecha, combo, autocompletado_id" />
    </list>
</main>