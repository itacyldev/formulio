<examples>
    <form id="form1" repoId="contacts">
        <permissions />
        <data>
            <repo type="csv" />
            <repo type="properties" />
            <repo type="db" />
            <repo type="photo" />
        </data>
        <list id="form1#list">
            <datatable />
            <datalist />
            <actions>
                <add route="formId#edit" />
            </actions>
        </list>
        <edit id="form1#edit" onsave="method2">
            <tabs>
                <tab>
                    <field onchange="method2" value="${entity.field}" />
                </tab>
                <tab>
                    <field />
                </tab>
            </tabs>
            <actions>
                <save to="form1#list" value="save">
                    <param name="1" value="${entity.contact_id}" />
                </save>
                <save value="saveAndStay" />
                <delete to="form1#list" value="save">
                    <param name="1" value="${entity.contact_id}" />
                </delete>
                <cancel value="save" />
                <nav to="form2#edit" value="next">
                    <param name="1" value="${entity.contact_id}" />
                </nav>
            </actions>
            <script engine="rhino">
                function method1(param) {

                ctx[entity.f3] = entity.f1 + entity.f2

                }
                function method2(param) {
                }

            </script>
        </edit>
    </form>

    <!-- formulario minimo -->
    <form>
        <data>
            <repo dbFile="relativeToDataFolderOrAbsolute/file.sqlite" tableName="tableName" />
        </data>
    </form>
    <!-- si el repositorio se ha registrado ya -->

    repo="mibd"
    <form dbTable="mitabla" dbfile="mibd.sqlite" />
    <form repo="mibd" />

    <listExamples>
        <list fields="field1,field2,field3">
            <!-- crear componente data table a partir de la información -->
        </list>
        <list expression="${entity.f1} - ${entity.f2.substring(0,10)}" />
        <list>
            <datatable filterFields="field2,field3" sortFields="field1,field2" />
        </list>
        <list>
            <datalist filterFields="field2,field3" sortFields="field1,field2" />
        </list>
    </listExamples>

    <form id="miform" repoId="myrepo">
        <list fields="field1,field2,field3">
            <!-- crear componente data table a partir de la información -->
        </list>
        <edit fields="*">
            <inputText value="${entity.field1}">
                <converter format="dd/MM/yyyy" type="date" />
            </inputText>
            <inputText value="${entity.field2}">
                <converter format="##0,##" type="numeric" />
                <validator type="number" />
            </inputText>
            <inputText mandatory="true" value="${entity.field3}">
                <validator maxLength="20" minLength="5" type="string" />
                <validator type="email" />
            </inputText>
            <!-- campo calculado -->
            <inputText readonly="true" value="${entity.field5}/${entity.field4}" />
        </edit>
    </form>

    <form>
        <data>
            <repo dbFile="relativeToDataFolderOrAbsolute/file.sqlite" tableName="tableName" />
        </data>
        <list expression="${entity.f1} - ${entity.f2.substring(0,10)}" />
    </form>
    <form>
        <data>
            <repo dbFile="relativeToDataFolderOrAbsolute/file.sqlite" id="repo1"
                tableName="tableName">
                <property name="booleanField" type="Boolean" />
                <property name="geometry" type="Geometry" />
            </repo>
        </data>
        <list>
            <datable>
                <col value="${entity.f1}" />
                <col value="${entity.f2} + ${entity.f3}" />
                <repofilter>

                </repofilter>
            </datable>
        </list>
    </form>

    <form repoId="repo1">
        <data>
            <!-- si hay más de uno, uno debe ser el principal y referenciado en el formulario-->
            <repo id="repo1" />
            <repo fields="f1,f2" id="repo2" />
            <repo dbFile="file.sqlite" id="repo3">
                <sql>select * from table_of_codes where code = ${form1.view.field1}</sql>
            </repo>
        </data>
        <edit fields="*">
            <!-- sobre cargamos la definición de uno de los campos para utilizar un lookup en uno y un compo en otro-->
            <select repo="repo3" value="${entity.field1}" />
            <inputText lookup="repo3" readonly="true" value="${entity.field2}"></inputText>
            <autocomplete repo="repo3" value="${entity.field3}" />

            <lookup id="lookup1" repo="repo3">
                <datable>
                    <col value="${entity.f1}" />
                    <col value="${entity.f2} + ${entity.f3}" />
                </datable>
            </lookup>
        </edit>
    </form>

    <form repoId="myrepo">

        <script>

        </script>
    </form>
</examples>
